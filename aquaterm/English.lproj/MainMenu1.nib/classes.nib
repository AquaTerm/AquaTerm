{
    IBClasses = (
        {
            ACTIONS = {
                cascadeWindows = id; 
                debug = id; 
                lineDrawingTest = id; 
                mailBug = id; 
                mailFeedback = id; 
                showAvailableFonts = id; 
                showHelp = id; 
                showPrefs = id; 
                stringDrawingTest = id; 
                testview = id; 
                tileWindows = id; 
            }; 
            CLASS = AQTController; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {refreshView = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {showInfo = id; }; 
            CLASS = GPTInfoDelegate; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}