{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                addPathAction = id; 
                addTextAction = id; 
                connectAction = id; 
                releaseAction = id; 
            }; 
            CLASS = MyObject; 
            LANGUAGE = ObjC; 
            OUTLETS = {statusOutlet = id; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}